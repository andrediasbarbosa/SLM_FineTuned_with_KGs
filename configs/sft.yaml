# Training config for B4: KG-grounded SFT on the KG-derived SFT dataset (Phase 6).
split: train
num_epochs: 3
batch_size: 4
learning_rate: 2.0e-4
max_seq_length: 512
warmup_steps: 50
logging_steps: 10
